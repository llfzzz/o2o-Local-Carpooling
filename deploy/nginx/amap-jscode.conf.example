# Copy outside the repository to /etc/nginx/secrets/o2o-amap-jscode.conf, replace the placeholder,
# chown it to root and chmod 600. Never commit the real JSAPI security code.
set $amap_jscode "replace-with-amap-jsapi-security-code";
